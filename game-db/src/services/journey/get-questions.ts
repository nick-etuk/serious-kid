import { Question, Snippet } from "../data";

export function getQuestions(snippets: Snippet[]): Question[] {
  /*
  Fetch children of snippets
  Fetch questions of those snippets.
  */
  return questions;
}
